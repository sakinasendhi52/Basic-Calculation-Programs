// 10. Write a program to take two string numbers and perform numeric addition after conversion.

let a:string="400";             //variable in string datatype
let b:string="600";             //variable in string datatype

let num1:number=Number(a);        //conversion into number datatype
let num2:number=Number(b);        //conversion into number datatype

console.log(`Adition of ${num1} and ${num2} is ${num1+num2}`);        //display the addition