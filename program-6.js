"use strict";
/*Write a program showing different TypeScript data types:
  a. number
  b. string
  c. boolean
*/
let num = 10;
let text = "This is text!";
let isFound = true;
console.log(`----------- TYPESCRIPT DATATYPES -----------`);
console.log(`Type of ${num} is ${typeof (num)}`);
console.log(`Type of ${text} is ${typeof (text)}`);
console.log(`Type of ${isFound} is ${typeof (isFound)}`);
console.log(`--------------------------------------------`);
