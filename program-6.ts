/* 6. Write a program showing different TypeScript data types:
   a. number
   b. string
   c. boolean
*/

let num:number=10;                    //variable in number type
let text:string="This is text!";      //variable in string type   
let isFound:boolean=true;             //variable in boolean type 

console.log(`----------- TYPESCRIPT DATATYPES -----------`);   //display different datatypes
console.log(`Type of ${num} is ${typeof(num)}`);        
console.log(`Type of ${text} is ${typeof(text)}`); 
console.log(`Type of ${isFound} is ${typeof(isFound)}`); 
console.log(`--------------------------------------------`);