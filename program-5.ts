// 5. Write a program to calculate area of rectangle using : length and Width

let len:number=10;             //length variable in number type
let width:number=5;            //width variable in number type

console.log(`----------- AREA OF RECTANGLE -----------`);
console.log(`Length = ${len}   Width = ${width}`);     //display length and width
console.log(`Area of Rectangle  =  ${len*width}`);     //display area
console.log(`-----------------------------------------`);